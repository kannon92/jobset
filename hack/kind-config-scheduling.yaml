kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
# NOTE: Do NOT set scheduling-specific feature gates at the cluster level.
# Kind's featureGates field pushes gates to ALL components including the
# kubelet, which may not recognize alpha scheduling gates. Instead, enable
# them only on the apiserver and scheduler via extraArgs.
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: ClusterConfiguration
        apiVersion: kubeadm.k8s.io/v1beta3
        apiServer:
          extraArgs:
            runtime-config: "scheduling.k8s.io/v1alpha2=true"
            feature-gates: "GenericWorkload=true,GangScheduling=true,TopologyAwareWorkloadScheduling=true,WorkloadAwarePreemption=true"
        scheduler:
          extraArgs:
            feature-gates: "GenericWorkload=true,GangScheduling=true,TopologyAwareWorkloadScheduling=true,WorkloadAwarePreemption=true"
  - role: worker
  - role: worker
  - role: worker
  - role: worker
